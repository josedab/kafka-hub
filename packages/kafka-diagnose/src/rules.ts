/**
 * @deprecated Import from ./rules/index instead.
 * This file exists only for backward compatibility with direct imports.
 */

import type { Rule } from "./types";
import { allRules } from "./rules/index";

export const rules: Rule[] = allRules;
