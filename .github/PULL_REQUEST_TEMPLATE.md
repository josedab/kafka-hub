## What

<!-- What does this PR change? Keep it short — the diff explains the how. -->

## Why

<!-- Why is this change needed? Link the issue if one exists. -->

Closes #

## Surface

- [ ] Learn
- [ ] Diagnose
- [ ] Simulate
- [ ] CLI
- [ ] Packages (`@kafka-hub/*`)
- [ ] Site / docs / infra

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm test` passes
- [ ] `pnpm build` passes
- [ ] If touching Diagnose rules: rule has a test + a `learnSlug` link
- [ ] If touching the simulator: deterministic (same inputs → same state)
- [ ] If touching MDX: frontmatter `date` is **quoted** (YAML gotcha)

## Notes for reviewers

<!-- Anything tricky, intentional, or worth flagging. -->
